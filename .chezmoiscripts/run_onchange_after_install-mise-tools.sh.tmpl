#!/bin/bash

# Runs after mise config is applied, re-runs when config changes
# config.toml hash: {{ include "dot_config/mise/config.toml" | sha256sum }}

if command -v mise &> /dev/null; then
    echo "Installing mise tools..."
    mise install -y
else
    echo "mise is not installed, skipping tool installation"
fi
